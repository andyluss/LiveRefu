# R01 · 文档约定 · 占位

> 状态：**占位**。本文暂为名目，正文以 [`tech/docs-convention.md`](../tech/docs-convention.md) 为准；
> 正式版待 [`lab/formal-system`](../lab/formal-system/README.md) 实验成熟后迁入替换本文件。

## 规则要点（当前）

- 简体中文，Markdown（UTF-8）。
- `doc/` 编号体例（`00_总论_…` 等）；`studio/` 角色体例。
- **相对链接深度**：朝向工作区根每深一级多一个 `../`（校验见 [R03](R03-link-validation.md)）。

## 未来

- 待 `lab/formal-system` 的形式化规则迁入后，替换为含「主选 + 备选 + 判定标准 + 演进历史」的正式版。
